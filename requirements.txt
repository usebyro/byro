# The real dependency list lives in Backend/requirements.txt, which is what CI
# (working-directory: Backend) and cloudbuild.yaml (dir: Backend) install.
# This file forwards to it so that running `pip install -r requirements.txt`
# from the repo root installs the same set — the two copies had silently
# diverged, which is how a missing dependency reached CI.
-r Backend/requirements.txt
